# Lista de Tarefas: Implementação do Módulo de BI

---
ID: 01
File Path: backend/prisma/schema.prisma
Context: Define a estrutura do banco de dados e índices gerados para o Prisma Client e migrações.
Action: Adicionar 4 índices compostos focados em BI para as colunas de arquivamento, status, unidade_id e processamento.
Logic Details: 
  - Criar `idx_casos_bi_status`: `@@index([arquivado, status], map: "idx_casos_bi_status")`
  - Criar `idx_casos_bi_unidade_status`: `@@index([arquivado, unidade_id, status], map: "idx_casos_bi_unidade_status")`
  - Criar `idx_casos_bi_tipo`: `@@index([arquivado, tipo_acao], map: "idx_casos_bi_tipo")`
  - Criar `idx_casos_bi_processed_at`: `@@index([processed_at], map: "idx_casos_bi_processed_at")`
Acceptance Criteria: O schema possui os 4 novos índices e pode ser persistido usando `npx prisma db push`.
---
ID: 02
File Path: backend/package.json
Context: Gerenciador de dependências do backend Node.
Action: Instalar dependência para a geração de arquivos Excel do BI.
Logic Details:
  - Adicionar a biblioteca `exceljs` para montar a resposta XLS em streaming via backend.
Acceptance Criteria: `package.json` atualizado contendo `exceljs`.
---
ID: 03
File Path: backend/src/controllers/casosController.js
Context: Concentra a lógica de listagem e resumo dos casos, consumida pelo Dashboard existente.
Action: Refatorar a função `resumoCasos` para utilizar agregações nativas em vez de processamento e loop em memória.
Logic Details: 
  - Substituir `findMany` em arrays inteiros por consultas Prisma/Supabase usando `groupBy`.
  - Corrigir a contagem de "representacao/proprio" utilizando `casos_partes.count` (Prisma).
  - Preservar rigorosamente o mesmo formato de resposta para garantir backward compat com o Dashboard.
Acceptance Criteria: Endpoint `resumoCasos` retorna a exata mesma estrutura sem degradar em performance com grandes volumes.
---
ID: 04
File Path: backend/src/controllers/biController.js
Context: Novo controller responsável pela geração estritamente server-side dos relatórios de BI.
Action: Criar arquivo com lógicas de extração massiva (`gerarRelatorio` e `exportarXlsx`).
Logic Details: 
  - Executar as 9 queries detalhadas no plano dentro de um único `Promise.all` e com os filtros de data e unidade validados.
  - Extração de tempo médio IA via query raw: `prisma.$queryRaw(SELECT AVG(EXTRACT(EPOCH FROM ...)))`.
  - Utilizar OBRIGATORIAMENTE `stringifyBigInts()` na reposta gerada para evitar falhas silenciosas de cast do `BigInt`.
  - Geração assíncrona do xlsx utilizando `ExcelJS` e dividindo abas com base nos widgets parametrizados.
Acceptance Criteria: Controlador expõe funções para query dinâmica agregada isenta de PII, e download fluído do arquivo `.xlsx`.
---
ID: 05
File Path: backend/src/routes/bi.js
Context: Novo arquivo de rotas que mapeia as saídas da API de BI para o frontend admin.
Action: Declarar rotas `/gerar` e `/export-xlsx` ligadas ao `biController`.
Logic Details: 
  - Criar middleware inline `requireAdmin` (`if(req.user.cargo !== 'admin') return res.status(403)`).
  - Rotear requisições validando payload dinâmico (filtros: período, unidade_id, etc).
Acceptance Criteria: Todas as requisições para `/api/bi/` barram usuários não-admins e prosseguem quando autorizadas.
---
ID: 06
File Path: backend/server.js
Context: Ponto de entrada do backend Express que registra e agrupa as rotas do app.
Action: Importar o arquivo de rotas do BI e montá-lo no stack do Express.
Logic Details: 
  - `const biRoutes = require('./src/routes/bi.js')` (ou import de módulo ESM correspondente).
  - Associar namespace via `app.use('/api/bi', biRoutes)`.
Acceptance Criteria: A rota `/api/bi/gerar` pode ser conectada de fato pelo Frontend.
---
ID: 07
File Path: frontend/package.json
Context: Gerenciador de dependências do frontend React + Vite.
Action: Adicionar as bibliotecas de dados e PDF necessárias.
Logic Details: 
  - Adicionar as bibliotecas `recharts`, `jspdf` e `html2canvas` aos pacotes de instalação via npm.
Acceptance Criteria: Dependências inseridas e utilizáveis pelos componentes do app.
---
ID: 08
File Path: frontend/src/areas/defensor/hooks/useBiData.js
Context: Novo custom hook React que gerenciará o fetch manual e estado das preferências de dashboard do Admin.
Action: Criar gerência de estado e funções de persistência.
Logic Details: 
  - Inicializar `filtros` (período padrão, 'todas' unidades) e `prefs` lidos da `localStorage["bi_prefs_v1"]`.
  - Criar os sub-métodos de requisição `gerar()`, `exportarXlsx()` e `exportarPdf()`.
  - `exportarPdf()` precisa incluir a ocultação temporária de ferramentas UI antes da chamada ao `html2canvas`.
Acceptance Criteria: Hook pronto para ser importado na página de `Relatorios.jsx` com a centralização total das regras.
---
ID: 09
File Path: frontend/src/areas/defensor/pages/Relatorios.jsx
Context: Tela central do módulo BI reservada apenas aos diretores e administradores do mutirão.
Action: Desenvolver o layout com filtros, status vazio e 7 painéis customizáveis (Widgets).
Logic Details: 
  - Implementar classes Tailwind v4 preservando temas locais.
  - Desenvolver seções iterando com componentes Recharts (Pie, Line e Bars) alimentados pelo `useBiData`.
  - Encapsular os blocos em um `<BiWidget>` contendo botão de enable/disable conectado às preferências (localStorage).
Acceptance Criteria: Gráficos exibem corretos sumários em tela baseados no período com UX agradável (inclusive PDF export).
---
ID: 10
File Path: frontend/src/areas/defensor/components/layout/Sidebar.jsx
Context: Menu de navegação lateral exibido após login da área de Defensor e Admin.
Action: Condicionar novo botão "Relatórios" somente se cargo corresponder a admin.
Logic Details: 
  - Identificar cargo em contexto (`const { user } = useAuth()`).
  - Adicionar item na lista de navegação: ícone `BarChart3`, direcionando para `/painel/relatorios`.
Acceptance Criteria: Visualizadores, defensores e estagiários não verão o link. Apenas admins possuem o acesso.
---
ID: 11
File Path: frontend/src/App.jsx
Context: Entrypoint React onde residem definições de Rotas do react-router-dom.
Action: Implementar a rota lazy para a nova página do relatórios.
Logic Details: 
  - Importar via código dinâmico: `const Relatorios = lazy(() => import('./areas/defensor/pages/Relatorios'))`.
  - Cadastrar nova entrada na árvore de rotas restritas `path="/painel/relatorios"`.
Acceptance Criteria: Usuário atinge `/painel/relatorios` no navegador e renderiza a tela.
---
ID: 12
File Path: /TESTE/VALIDACAO
Context: Passo final para garantir estabilidade ponta-a-ponta e compliance de infra.
Action: Executar roteiro de validação.
Logic Details: 
  - Aplicar Prisma Push DB e validar no console os índices aplicados.
  - Subir dev servers. Tentar acessar `/api/bi/gerar` deslogado e testar bloqueio JWT.
  - Navegar na URL do painel logado com Defensor simulando acesso direto e constatar o Redirecionamento 403.
  - Entrar como Admin, gerar consulta customizada e exportar XLSX. Checar abas por widgets e garantir zero PII exportada.
Acceptance Criteria: BI gerado com alta fluidez (< 1 seg), total vedação de dados sensíveis LGPD, e layout estável no print em PDF.
---
ID: 13
File Path: backend/src/controllers/biController.js
Action: Criar funÃ§Ã£o `exportarXlsxLote`.
Logic Details:
  - Iterar sobre todas as unidades ativas.
  - Rodar as mesmas 8 queries do BI por `unidade_id` (Q4 removida).
  - Criar uma aba por unidade no workbook.
  - Definir nome da aba com `unidade.nome`, truncado em 31 caracteres.
  - Gerar um Ãºnico arquivo no formato `bi-lote-YYYY-MM-DD.xlsx`.
  - Usar ExcelJS streaming igual ao export individual.
Route: `POST /api/bi/export-xlsx-lote`
Acceptance Criteria: O admin consegue baixar um Ãºnico XLSX contendo uma aba por unidade ativa.
---

ID: 14
File Path: backend/src/routes/bi.js
Context: Ajuste de seguranÃ§a e escopo do mÃ³dulo BI.
Action: Revisar autorizaÃ§Ã£o do BI para contemplar coordenador com escopo restrito.
Logic Details:
  - O acesso nÃ£o deve ficar preso apenas a `cargo === 'admin'`.
  - Permitir acesso tambÃ©m para coordenador com flag de BI ativa.
  - Se o usuÃ¡rio for coordenador, forÃ§ar consulta apenas Ã  sua unidade/regional autorizada.
  - Bloquear exportaÃ§Ã£o em lote para nÃ£o-admin com `403`.
Acceptance Criteria: Admin mantÃ©m visÃ£o global; coordenador autorizado vÃª apenas o seu escopo.
---

ID: 15
File Path: frontend/src/areas/defensor/hooks/useBiData.js
Context: Ajuste do fluxo de exportaÃ§Ã£o do XLSX protegido por JWT.
Action: Alterar `exportarXlsx()` para usar `POST` autenticado com download via `blob`.
Logic Details:
  - NÃ£o usar link nativo nem navegaÃ§Ã£o direta para exportar XLSX.
  - Fazer `fetch('/api/bi/export-xlsx', { method: 'POST', headers: { Authorization, Content-Type } })`.
  - Enviar os filtros no body da requisiÃ§Ã£o.
  - Converter a resposta em `blob`, criar `URL.createObjectURL(blob)` e disparar o download programaticamente.
Acceptance Criteria: O XLSX baixa corretamente sem perder o header `Authorization`.
---

ID: 16
File Path: frontend/src/areas/defensor/pages/Relatorios.jsx
Context: Ajustes funcionais aprovados para o painel BI.
Action: Atualizar a tela para refletir o novo escopo do mÃ³dulo.
Logic Details:
  - Remover o widget de perfil/representaÃ§Ã£o vs prÃ³prio.
  - Adicionar botÃ£o de exportaÃ§Ã£o em lote quando o usuÃ¡rio for admin.
  - Se o usuÃ¡rio for coordenador autorizado, travar filtros para sua unidade/regional.
Acceptance Criteria: A UI nÃ£o exibe o widget removido, respeita escopo por unidade e apresenta exportaÃ§Ã£o em lote apenas para admin.
---

ID: 17
File Path: frontend/src/areas/defensor/components/layout/Sidebar.jsx
Context: Controle de visibilidade do acesso ao BI.
Action: Ajustar a exibiÃ§Ã£o do item "RelatÃ³rios" para usar permissÃ£o explÃ­cita de BI.
Logic Details:
  - Continuar permitindo acesso para admin.
  - Exibir o item tambÃ©m para coordenador com permissÃ£o de BI ativa.
  - NÃ£o exibir para os demais perfis.
Acceptance Criteria: O link do BI aparece apenas para quem realmente pode entrar no mÃ³dulo.
---
