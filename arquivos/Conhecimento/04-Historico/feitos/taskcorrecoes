- `[ ]` **ID: 01**
  File Path: `backend/src/controllers/casosController.js`
  Context: Mapeia as entidades do banco (casos, partes, juridico) para gerar o objeto "enriched" usado pelas minutas.
  Action: Corrigir o mapeamento das variáveis `empregador_nome` e `CIDADEASSINATURA` no `mapCasoRelations`.
  Logic Details: 
  - Para `empregador_nome`: deve priorizar `juridico.empregador_nome`, falhando para `dadosFormulario.empregador_nome`, em seguida `dadosFormulario.empregador_requerido_nome`.
  - Para `CIDADEASSINATURA`: deve priorizar `juridico.cidade_assinatura`, falhando para `dadosFormulario.CIDADEASSINATURA`, depois `dadosFormulario.cidade_assinatura`, e por fim `caso.unidade?.comarca`.
  - Associar tanto `CIDADEASSINATURA` quanto `cidade_assinatura` no objeto `enriched`.
  Acceptance Criteria: Se a tabela `casos_juridico` estiver recém-criada (vazia de empregador), a minuta herda o valor corretamente do `dados_formulario`.

- `[ ]` **ID: 02**
  File Path: `backend/src/controllers/casosController.js`
  Context: Endpoint que reconstrói a petição Word (`docx`) substituindo as tags usando `docxtemplater`.
  Action: Alterar a query `findUnique` dentro de `regerarMinuta`.
  Logic Details: 
  - Adicionar na query atual (`include: { partes: true, ia: true, juridico: true }`) o include da unidade: `unidade: { select: { comarca: true } }`.
  - Isso garantirá que o fallback extremo do `CIDADEASSINATURA` configurado na Task 01 funcione quando `regerarMinuta` é acionado.
  Acceptance Criteria: O `caso` recuperado pelo backend contém os dados da comarca na tabela `unidades` para injeção final.

- `[ ]` **ID: 03**
  File Path: `frontend/src/areas/servidor/components/secoes/SecaoProcessoOriginal.jsx`
  Context: Seção do formulário de triagem responsável pelos dados do processo original.
  Action: Remover o campo `<input>` referente ao `link_calculadora`.
  Logic Details: 
  - Deletar as linhas que contêm o `input` com `id="link_calculadora"` e seu respectivo `onChange/value`.
  - Manter a `label` "Link da Calculadora (Opcional)" apenas com a tag âncora `<a>` que redireciona para o Dr. Calc.
  - Não alterar a estrutura visual do Grid.
  Acceptance Criteria: A seção da calculadora possui apenas um texto link clicável e não permite inserção de dados pelo usuário.

- `[ ]` **ID: 04**
  File Path: `frontend/src/areas/servidor/hooks/useFormEffects.js`
  Context: Hook que realiza o auto-save do estado do formulário de triagem a cada 3 segundos no `localStorage`.
  Action: Interromper o auto-save se o caso já houver sido submetido com sucesso.
  Logic Details: 
  - Dentro do timer no `useEffect` de auto-save, verificar se os dados do formulário indicam uma conclusão. Alternativamente, verificar se os arquivos/áudios estão zerados ou um flag específico do React.
  - A forma mais segura: verificar se a rota alterou ou se o `formState.isSubmitting` / ausência de campos chave pós-sucesso justificam retornar (return) antes de gravar em `localStorage.setItem`.
  - Modificar o script para que a transição limpa feita pelo `submissionService.js` (ao invocar `localStorage.removeItem`) não seja sobrescrita.
  Acceptance Criteria: Um "rascunho fantasma" não é criado na máquina do atendente logo após ele enviar um caso e acessar a tela de "Atendimento Gerado".

- `[ ]` **ID: 05**
  File Path: `Manual / Validação Integrada`
  Context: Teste de garantia de qualidade (QA) pós-implementação.
  Action: Validar a persistência, o cache e o regerar minuta.
  Logic Details: 
  - Preencher uma triagem de Execução Cumulada com nome de empregador e cidade.
  - Submeter, verificar o `localStorage` do navegador para confirmar se está vazio após a tela de sucesso.
  - Ir no painel do Defensor, "Travar" o atendimento, editar um valor qualquer de penhora na aba "Informações Financeiras (Cumulado)" e clicar em "Regerar Minuta com Novos Dados".
  - Fazer download da minuta final.
  Acceptance Criteria: O documento `.docx` não exibe "______" nos lugares de Empregador e Cidade. O localStorage da triagem continua limpo.
